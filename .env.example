# Neon Postgres connection string (pooler or direct)
DATABASE_URL=

# Better Auth — use `openssl rand -base64 32` (min 32 chars)
BETTER_AUTH_SECRET=

# App URL where Better Auth is served (no trailing slash)
BETTER_AUTH_URL=http://localhost:3000
